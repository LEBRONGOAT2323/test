<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     xmlns:xhtml="http://www.w3.org/1999/xhtml"
     width="100%" height="100%" viewBox="0 0 1920 1080">

  <foreignObject id="fo" width="1920" height="1080">

    <xhtml:html style="width:100%;height:100%;margin:0;padding:0;">

      <xhtml:head>
        <xhtml:style>
          html,body{
            width:100%;
            height:100%;
            margin:0;
            overflow:hidden;
            background:#000;
          }

          iframe{
            position:fixed;
            inset:0;
            width:100%;
            height:100%;
            border:none;
          }
        </xhtml:style>
      </xhtml:head>

      <xhtml:body>

        <xhtml:iframe id="frame" src="about:blank"/>

        <xhtml:script>
        //<![CDATA[
        (() => {

          function resize(){
            const w=innerWidth,h=innerHeight;

            document.querySelector("svg")
              .setAttribute("viewBox",`0 0 ${w} ${h}`);

            document.querySelector("svg")
              .setAttribute("width",w);

            document.querySelector("svg")
              .setAttribute("height",h);

            document.getElementById("fo")
              .setAttribute("width",w);

            document.getElementById("fo")
              .setAttribute("height",h);
          }

          resize();
          addEventListener("resize",resize);

          (async()=>{

            const response=await fetch("g.html?t="+Date.now());
            const html=await response.text();

            const frame=document.getElementById("frame");
            const doc=frame.contentDocument||frame.contentWindow.document;

            doc.open();
            doc.write(html);
            doc.close();

          })();

        })();
        //]]>
        </xhtml:script>

      </xhtml:body>

    </xhtml:html>

  </foreignObject>

</svg>
